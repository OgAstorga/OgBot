#include <bits/stdc++.h>

using namespace std;

pair<int, int> lastMove;
string inputBoard[9];

int main() {
  // Read input
  cin >> lastMove.first >> lastMove.second;
  for (int i=0; i<9; ++i) {
    cin >> inputBoard[i];
  }

  return 0;
}
